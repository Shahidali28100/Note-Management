/**
 * Placeholder scaffold page (AB-1001). Proves the Vite dev server, the build,
 * and Tailwind all work end-to-end — real pages/routes start at AB-1010+.
 */
export function HomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <div className="text-center">
        <h1 className="text-2xl font-semibold">Note Management</h1>
        <p className="mt-2 text-muted-foreground">Project scaffold — AB-1001</p>
      </div>
    </main>
  )
}
